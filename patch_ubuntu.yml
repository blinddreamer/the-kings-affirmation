- hosts: ubuntu
  remote_user: "{{ noob_user }}"
  become: yes
  become_user: "{{ voltron }}"
  become_pass: "{{ voltron_pwd }}"
  vars_files:
  - vars.yml
  tasks:
  - name: PLS UPDATE THIS BRO
    apt:
      update_cache: yes
      upgrade: 'yes'
    register: apt_update_status

  - name: DELETE OLD SHIT
    apt:
      autoremove: yes

  - name: REBOOT.me
    reboot:
      post_reboot_delay: 60
    when: apt_update_status.changed